version: '3'

services:
  www:
    build: './src/www'
    volumes:
      - './src/www:/app'
    ports:
      - '8080:80'
  api:
    build: './src/api'
    volumes:
      - './src/api:/app'
    ports:
      - '8000:80'
